server:
  port: 8082

spring:
  application:
    name: order-service

  zipkin:
    base-url: http://localhost:9411
    sender:
      type: web
    sampler:
      probability: 1.0

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8762/eureka/
    register-with-eureka: true
